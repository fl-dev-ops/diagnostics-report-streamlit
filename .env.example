# Read-only Postgres connection (reuse the diagnostics app DATABASE_URL).
# The app only ever issues SELECT queries.
DATABASE_URL="postgresql://user:password@host/dbname?sslmode=require"

# Base URL used to build report / recording links.
REPORT_BASE_URL="https://diagnostics.intervoo.ai"
